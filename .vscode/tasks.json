{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile CPCReady",
            "type":"shell",
            "command": "./build.sh",
            "problemMatcher": ["$msCompile"],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}